
// all the imports here
import { Router } from "express";

// creating a router object
const router: Router = Router();


// exporting the router object as checkout routes
export const checkoutRoutes = router;